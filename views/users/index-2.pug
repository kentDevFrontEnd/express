extends ../layouts/common.pug

block content
	h1 Users List
	a(href="/users/create") New
	form(action="users/search", method="get", class="d-flex")
		input(type="text", name="q", class="form-control")
		button(class="btn-primary ml-3") Search
	ul
		each user in users
			li
				span= user.name
				span &nbsp;
				a(href='/users/'+user.id) View